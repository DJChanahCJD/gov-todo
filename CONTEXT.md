# gov-todo

面向体制内办公场景的个人 TODO 工具。极简、高效、沉浸、易用。

## Language

**任务 (Task)**:
用户需要记录和跟踪的一项待办事项。有且仅有三种生命周期状态：活跃 → 已完成 → 已归档。活跃任务不可删除（必须先完成后归档）；已归档任务支持删除，需二次确认。
_Avoid_: 事项、待办、条目

**活跃任务 (Active Task)**:
尚未完成的任务，显示在四象限视图中。是任务的初始状态。
_Avoid_: 未完成任务、进行中任务

**已完成任务 (Done Task)**:
用户标记为完成后立即归档的任务。是活跃 → 已归档的瞬时过渡状态，在界面上短暂闪现后即转入归档视图。
_Avoid_: 完成的任务、已办任务

**已归档任务 (Archived Task)**:
从活跃视图移除、进入历史记录的任务。仅可查看，不可修改（但支持删除，需二次确认）。归档任务位于四象限区域下方，按完成日期倒序分组显示（如"今天"、"昨天"、"7月20日"……），每条记录仅展示标题和完成日期。
_Avoid_: 历史任务、旧任务

**任务属性 (Task Fields)**:
一个任务包含以下字段：唯一标识(id)、标题(title)、截止时间(deadline, 可选)、象限(quadrant, 0-3)、是否置顶(pinned, 布尔)、创建时间(createdAt)、完成时间(completedAt, 完成时记录)。无描述(description)字段，无独立的归档时间(完成即归档)。创建后的任务允许编辑标题和截止时间。
_Avoid_: 添加不必要的冗余字段

**截止时间 (Deadline / DDL)**:
任务必须完成的目标日期。一个任务最多有一个截止时间。活跃任务在象限内默认按截止时间升序排列（最临近的排最前）。
_Avoid_: 到期日、截止日期、due date

**周期任务模板 (Recurring Template)**:
一种特殊的任务规则，按 cron 表达式定义的周期自动生成任务实例。模板本身不是活跃任务，不显示在象限中；每次生成的具体实例是独立的任务，拥有独立的生命周期。模板包含标题(title)、描述(description)、默认象限(quadrant)和 cron 表达式。UI 层面以友好选项呈现（每日/每周/每月/每年/自定义），底层以 cron 存储。模板的创建和管理通过侧边按钮 + 弹窗进行。
_Avoid_: 重复任务、循环任务、定时任务

**周期实例 (Recurring Instance)**:
由周期任务模板生成的具体任务，拥有独立生命周期，可独立完成和归档。实例按需生成——应用启动时检查模板，生成当前时间之前尚未生成的实例。每个实例的截止时间由模板规则和当前周期共同计算得出。

**置顶 (Pin)**:
将某活跃任务固定在象限内的顶部位置。被置顶的任务不受默认排序影响，始终显示在同象限内最上方。
_Avoid_: 优先、加星、收藏

**象限 (Quadrant)**:
活跃任务按"重要性 × 紧急性"两个维度划分的四个区域。每个活跃任务必须归属于且仅归属于一个象限。新建任务默认归属"重要不紧急"象限。用户可通过拖拽调整任务所属象限。
四个象限：重要且紧急、重要不紧急、不重要紧急、不重要不紧急。
_Avoid_: 分区、分类、类别
